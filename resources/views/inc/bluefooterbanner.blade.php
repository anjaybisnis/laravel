<!-- Start cta-section -->
<section class="cta-section2 section-gap primary-color-bg">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-6 text-center">
                <h3 class="text-uppercase text-white">{{ HP::set()->hpfbhtext }}</h3>
                <p class="pt-30 pb-30 mb-0 text-white">{{ HP::set()->hpfbstext }}</p>
                <a href="{{ HP::set()->hpfbburl }}" class="primary-btn">{{ HP::set()->hpfbbtext }}</a>
            </div>
        </div>
    </div>
</section>
<!-- End cta-section -->
